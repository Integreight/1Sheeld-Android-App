package com.integreight.onesheeld.utils;

import android.app.Activity;
import android.app.AlertDialog;
import android.content.DialogInterface;
import android.widget.ArrayAdapter;

public class PinDialogs {
	public static void showDigitalArduinoPins(Activity activity, DialogInterface.OnClickListener listener) {
//		AlertDialog.Builder dialogBuilder = new AlertDialog.Builder(activity);
//		dialogBuilder.setTitle("Connect With").setCancelable(false)//.setItems(ArduinoPin.getDigitalPins(),listener);
//		.setAdapter(new ArrayAdapter<ArduinoPin>(activity, R.layout.dialog_row, ArduinoPin.getDigitalPins()), listener);
//		dialogBuilder.show();
	}
}
